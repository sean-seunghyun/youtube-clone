extends base

block header
    h1 login Page

block content

    if logInError
        span=logInError
    form(method='post')
        input(type="text" name="emailAddress", placeholder="e-mail Address", required, maxlength=128)
        input(type="password" name="password", placeholder="password", required)
        input(type="submit" value="logIn")
        a(href="/users/github/start") Github login →